# Add Notes 功能使用示例

## 快速开始

### 步骤 1: 创建 Marker

使用 FIB Panel 或工具栏按钮创建一个 marker（例如 CUT_0）

### 步骤 2: 右键点击 Marker

在 FIB Panel 的 Markers 列表中，右键点击 `CUT_0`

```
┌─────────────────────────────────┐
│ Markers                         │
├─────────────────────────────────┤
│ CUT_0 - CUT - (100,200)..  ◄── 右键点击
│ CONNECT_0 - CONNECT - ...       │
│ PROBE_0 - PROBE - (300,400)     │
└─────────────────────────────────┘
```

### 步骤 3: 选择 "Add Notes"

右键菜单会显示：

```
┌─────────────────────┐
│ Zoom to Fit         │
│ Copy Coordinates    │
│ Add Notes          │ ◄── 点击这里
│ Rename Marker       │
│ Delete Marker       │
└─────────────────────┘
```

### 步骤 4: 输入 Notes

弹出对话框：

```
┌──────────────────────────────────────┐
│ Add Notes - CUT_0                    │
├──────────────────────────────────────┤
│ Enter notes for this marker:         │
│                                      │
│ ┌──────────────────────────────────┐ │
│ │ Critical area - low beam current │ │
│ └──────────────────────────────────┘ │
│                                      │
│           [ OK ]    [ Cancel ]       │
└──────────────────────────────────────┘
```

输入你的备注，例如：
- `Critical area - low beam current`
- `Beam: 30kV, Current: 10pA`
- `Contact pad for testing`

### 步骤 5: 保存并确认

点击 OK 后，会显示确认消息：

```
┌──────────────────────────────────────┐
│ Notes Updated                        │
├──────────────────────────────────────┤
│ Notes for CUT_0:                     │
│                                      │
│ Critical area - low beam current     │
│                                      │
│ Notes will be included in PDF export.│
│                                      │
│                [ OK ]                │
└──────────────────────────────────────┘
```

## 导出 PDF 查看 Notes

### 步骤 6: 导出 PDF

点击 FIB Panel 中的 "Export PDF" 按钮

### 步骤 7: 查看报告

在生成的 HTML/PDF 报告中，notes 会显示在 marker 信息区域：

```html
┌─────────────────────────────────────────────────────┐
│ CUT_0                                               │
├─────────────────────────────────────────────────────┤
│ Type: CUT                                           │
│ Coordinates: (100.00,200.00) to (150.00,250.00) μm │
│ Notes: Critical area - low beam current             │ ◄── 这里显示 notes
├─────────────────────────────────────────────────────┤
│ [Overview]  [Zoom 2x]  [Detail]                     │
│   (screenshots)                                     │
└─────────────────────────────────────────────────────┘
```

## 实际使用案例

### 案例 1: FIB 切割参数记录

```
Marker: CUT_0
Notes: Beam 30kV, 10pA, Pattern: Line, Dwell 1us
```

用途：记录 FIB 切割时使用的具体参数，便于后续重复实验。

### 案例 2: 样品区域标记

```
Marker: CONNECT_1
Notes: Device A - Metal layer 3, avoid oxide layer
```

用途：标记样品的特定区域和注意事项。

### 案例 3: 测试点说明

```
Marker: PROBE_2
Notes: Electrical test point - GND connection
```

用途：说明探针接触点的用途和连接信息。

### 案例 4: 工艺流程记录

```
Marker: CUT_3
Notes: Step 1: Rough cut (high current)
       Step 2: Fine cut (low current)
       Step 3: Polish (ultra-low current)
```

用途：记录多步骤工艺流程。

## 编辑和删除 Notes

### 编辑现有 Notes

1. 右键点击已有 notes 的 marker
2. 选择 "Add Notes"
3. 对话框会显示当前的 notes
4. 修改后点击 OK

### 删除 Notes

1. 右键点击 marker
2. 选择 "Add Notes"
3. 清空输入框
4. 点击 OK

删除后，PDF 报告中会显示 `Notes: -`

## 保存和加载

### 保存项目

点击 "Save" 按钮保存项目时，notes 会自动保存到 JSON 文件：

```json
{
  "markers": [
    {
      "id": "CUT_0",
      "notes": "Critical area - low beam current",
      ...
    }
  ]
}
```

### 加载项目

点击 "Load" 按钮加载项目时，notes 会自动恢复。

## 提示和技巧

### 1. 使用简洁的描述

✅ 好的 notes:
```
Beam: 30kV, 10pA, Dwell: 1us
```

❌ 过长的 notes:
```
This is a very important cutting area where we need to use 
a low beam current because the material is sensitive and 
we don't want to damage it...
```

### 2. 使用标准格式

建议使用统一的格式，例如：
```
Parameter: Value, Parameter: Value
```

### 3. 包含关键信息

- 工艺参数（电压、电流、时间）
- 材料信息
- 注意事项
- 测试结果

### 4. 定期保存

添加 notes 后记得保存项目，避免丢失信息。

## 常见问题

### Q: Notes 有长度限制吗？

A: 理论上没有限制，但建议保持简洁（1-2 行）以便在 PDF 中清晰显示。

### Q: 可以使用特殊字符吗？

A: 可以，但避免使用 HTML 特殊字符（`<`, `>`, `&`），可能影响 PDF 显示。

### Q: Notes 会显示在 GDS 文件中吗？

A: 不会。Notes 只保存在 JSON 项目文件中，不会写入 GDS 布局。

### Q: 可以批量添加 notes 吗？

A: 当前版本需要逐个添加。批量功能可能在未来版本中添加。

## 快捷操作

1. **双击 marker** → 缩放到 marker 位置
2. **右键 → Add Notes** → 添加备注
3. **右键 → Copy Coordinates** → 复制坐标
4. **右键 → Delete Marker** → 删除 marker

---

**开始使用 Add Notes 功能，让你的 FIB 工作流程更加高效！** 📝
